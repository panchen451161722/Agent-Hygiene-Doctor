# Agent Hygiene CLI

Read-only inventory and hygiene diagnostics for Codex, Claude Code, and Hermes installations.

## Usage

```text
agent-hygiene doctor --format json
agent-hygiene setup --dry-run
```