import type { AgentAdapter, AdapterResult } from "./adapter.js";
import type { ScanContext } from "./context.js";
export const runAdapters = async (adapters: readonly AgentAdapter[], context: ScanContext): Promise<readonly AdapterResult[]> => Promise.all(adapters.map(async (adapter) => { try { return await adapter.scan(context); } catch { return { agent: adapter.agent, inventory: [], diagnostics: [], coverage: "unknown" as const }; } }));