import type { ReportV1 } from "../core/report.js";
export const renderTerminal = (report: ReportV1): string => `Agent Hygiene ${report.schemaVersion}\nFindings: ${report.summary.findings.info + report.summary.findings.warning + report.summary.findings.error}\n`;